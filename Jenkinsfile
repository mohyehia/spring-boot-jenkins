pipeline {
  agent any
  stages {
    stage('Git Checkout') {
      steps {
        echo 'Git Checkout Completed!'
      }
    }

    stage('Build') {
      steps {
        echo 'Build Completed!'
      }
    }

    stage('Test') {
      steps {
        echo 'Test Completed!'
      }
    }

    stage('Deploy') {
      steps {
        echo 'Deploy Completed!'
      }
    }

  }
}